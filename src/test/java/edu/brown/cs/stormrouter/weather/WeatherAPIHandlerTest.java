package edu.brown.cs.stormrouter.weather;

import org.junit.Test;

import static org.junit.Assert.*;

public class WeatherAPIHandlerTest {

  @Test
  public void testGetWeather() throws Exception {
    /*WeatherConditions weather = WeatherAPIHandler.getWeather(42.3601, -71.0589);
    assertNotNull(weather);

    assertEquals(weather.getLatitude(), 42.3601, 0.0001);
    assertEquals(weather.getLongitude(), -71.0589, 0.0001);

    System.out.println("Timezone: " + weather.getTimezone());

    HourlyWeather hourly = weather.getHourly();
    assertNotNull(hourly);

    System.out.println("Summary: " + hourly.getSummary());
    System.out.println("Icon: " + hourly.getIcon());

    TimePoint[] data = hourly.getData();
    assertNotNull(data);

    System.out.println("Data Length: " + data.length);

    for (TimePoint time : data) {
      System.out.println(time);
    }*/
  }
}
